# 보정점 가림·화면 잘림 수정

리비전: `2026-09-02-visible-calibration-targets-1`

- 기존 점 중심은 화면 폭·높이의 2.5%와 97.5%에 고정됐습니다. 짧거나 작은 브라우저 화면에서는 38px 원의 반지름·테두리까지 고려하지 않아 일부가 화면 밖으로 잘릴 수 있었습니다.
- 보정점의 `z-index`가 보정 안내 HUD보다 낮아, 화면 크기에 따라 안내 문구와 겹치는 점이 가려질 수 있었습니다.
- 가장자리 보정점은 원래 2.5% 위치와 48 CSS px 중 더 안쪽인 위치를 사용합니다. 46px 원과 8px 외곽 강조 및 8px 안전 여백이 화면 안에 들어옵니다.
- 서버가 현재 브라우저 크기로 9개의 정확한 픽셀 좌표를 한 번 계산합니다. 브라우저 표시, EyeTrax, GazeFollower, EyeGestures, WebGazer 초기 보정이 모두 이 좌표를 그대로 사용합니다.
- 보정점은 안내 UI보다 위에 표시하고 색·중앙점을 강화했습니다. 실제 DOM 사각형이 화면 안전 여백을 벗어나면 보정을 진행하지 않고 구체적인 오류를 표시합니다.
- 200×200부터 3840×2160까지의 화면에서 아홉 점의 고유성·순서·가시 영역을 검사했습니다. 기존 공통 보정, 모델별 1회 fitting, WebGazer 추가 학습, 고정 실루엣, 응답 복구는 유지했습니다.

적용: 기존 서버 종료 → 이 ZIP을 새 폴더에 전체 해제 → 기존 Python 3.11 환경에서 `run.py` 실행 → 브라우저를 새로고침합니다. 패키지 재설치는 필요 없습니다.

---

# 프레임 응답 유실 복구 및 전송 로그

리비전: `2026-08-31-frame-reply-recovery-1`

- 첨부 로그에는 프레임 55의 모델 처리 완료와 서버 종료만 있고 Python 예외/네이티브 충돌은 없습니다. 실제 연결 실패 원인을 특정하지 않았습니다.
- 확인한 코드 결함: 응답 본문 쓰기의 BrokenPipeError/ConnectionResetError를 기록 없이 무시했습니다. 이제 헤더/본문 전송 전체의 OSError를 프레임 번호와 함께 기록하고, 일부 전송된 연결에 두 번째 HTTP 오류 응답을 보내지 않습니다.
- 서버가 완료한 최근 4개 프레임 응답을 전송 전에 메모리에 저장합니다. 브라우저가 같은 세션·프레임 번호의 응답만 GET으로 읽어 복구합니다. 입력 영상 재전송, 모델 재실행, 보정 표본 중복 추가는 없습니다.
- 실제 모델 오류·없는 응답·다른 세션·다른 프레임은 복구 성공으로 처리하지 않습니다. 복구 조회에는 인증 토큰이 필요합니다.
- 매 프레임 TCP 연결을 새로 열던 HTTP/1.0 대신 HTTP/1.1을 사용합니다. 본문 길이와 거부된 요청의 연결 종료를 처리합니다.
- 상태 `complete`를 `processed`로 변경해 모델/JSON 처리 완료와 브라우저 수신을 구분했습니다. 브라우저도 응답 헤더 전 실패와 본문 읽기 실패를 나눠 표시합니다.
- 공통 9점 보정, WebGazer 추가 학습, 모델 선택, 응시점, 웹캠+고정 실루엣, Python 의존성과 동봉 모델은 그대로입니다. 평가 버튼은 추가하지 않았습니다.

적용: 기존 서버 종료 → ZIP 전체를 새 폴더에 해제 → 기존 Python 3.11 환경에서 `run.py` 실행 → 터미널의 `http://127.0.0.1:8765/` 주소에 직접 접속. 이번 수정으로 인한 패키지 재설치는 필요 없습니다.

검증은 모형 모델·인메모리 HTTP 스트림·가짜 fetch로 수행했습니다. 실제 서버/브라우저/웹캠/모델 추론은 실행하지 않았습니다.

---

이전 변경 이력:

# 화면 단순화와 자동 오류 기록

리비전: `2026-08-31-simple-gui-automatic-logs-1`

- 12점 평가, 자세 조건 선택, 결과 저장/비교, 보정 기록 내보내기, 수동 연결 진단 버튼을 제거했습니다. `compare_results.py`, 평가 결과 처리 모듈과 HTTP 경로도 제거했습니다.
- 공통 9점 초기 보정, 네 엔진 선택, 응시점, 웹캠 미리보기, 고정 머리·실루엣은 유지했습니다. 초기 보정은 같은 PNG 프레임과 목표 좌표를 공유하며 내부 계수는 엔진별로 학습합니다.
- WebGazer의 사용자 클릭·마우스 이동 추가 학습을 유지했습니다. 모델 전환만으로 보정·추가 학습 상태·기준 윤곽을 초기화하지 않습니다.
- Python 예외를 `logs/runtime.log`에, 모델 호출 직전 단계를 `logs/last_operation.json`에 자동 기록합니다. 서버를 다시 시작하면 직전 단계를 별도 보관합니다.
- `faulthandler`로 가능한 네이티브 충돌과 60초 이상 걸린 요청의 스레드 스택을 `logs/native_fault.log`에 기록합니다. 진단 타이머가 모델을 종료하거나 학습 상태를 바꾸지는 않습니다. 모든 강제 종료가 기록되는 것은 아닙니다.
- 프레임의 연결 실패/응답 시간 초과 시 읽기 전용 서버 상태만 한 번 확인합니다. 프레임 POST를 재전송하거나 자동 재보정하지 않습니다.
- 지금 받은 로그에는 실제 예외나 종료 코드가 없습니다. 이 변경은 사용자 PC의 `/api/frame` 실패 원인이 해결됐다고 검증한 수정은 아닙니다.

## 유지한 이전 수정

- GazeFollower 컴포넌트 생성 전에 `Log.init()` 호출, 재보정 시 핸들러 재사용.
- Windows MediaPipe의 잘못된 절대 경로 결합 회피: EyeTrax 모델을 `model_asset_buffer`로 전달. 원본 특징 추출·깜빡임·학습·예측 코드는 상속 그대로 사용.
- 초기화 실패 뒤 이전 세션 ID로 종료 요청을 보내던 후속 400 방지.
- 전체 브라우저 JS/WASM/data/그래프/소스맵/회귀 모듈, FaceLandmarker와 MGazeNet 모델, 원본 아카이브와 라이선스 동봉.
- 원본 0바이트 MediaPipe data를 정상 파일로 인정하고 존재/크기/SHA-256으로 검증.
- 실행 중 자동 다운로드 없음. 오프라인 브라우저 파일 복원 가능. Python 의존성·버전 변경 없음.

## 적용

기존 서버 종료 → ZIP 전체를 새 폴더에 해제 → 기존 Python 3.11 환경에서 `run.py` 직접 실행 → 터미널의 HTTP 주소에 직접 접속. 패키지가 이미 설치된 동일 환경이면 재설치할 필요 없습니다. 브라우저 새로고침도 필요합니다.

제작 과정에서 앱·서버·브라우저·웹캠·모델 추론을 실행하지 않았습니다. 검증 범위는 `VALIDATION.md`에 기록했습니다.
