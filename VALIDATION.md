# 검증 범위

리비전: `2026-09-02-visible-calibration-targets-1`

Python 3.11 한 환경을 대상으로 하며 기존 고정 의존성을 바꾸지 않았습니다. EyeTrax 0.4.0, GazeFollower 1.0.2, EyeGestures 3.2.4(v3), WebGazer 3.5.3입니다.

## 앱을 시작하지 않는 코드 검사

- Python 테스트 39개: 공통 보정 8개, 보정점 배치 6개, GazeFollower 로거 1개, HTTP/파일 상태 3개, 파일 무결성 2개, 동봉 파일 오프라인 복원 1개, EyeTrax 바이트 모델 생성 5개, 자동 로그 4개, 프레임 응답 복구/전송 9개.
- 보정점 배치 검사는 200×200, 320×240, 800×600, 1366×768, 1920×1080, 3840×2160 CSS 픽셀에서 아홉 점이 모두 화면 안에 있고 서로 다르며 지정 순서를 유지하는지 확인했습니다. 46px 원·8px 외곽 강조·8px 안전 여백보다 최소 inset이 큰지, 보정점 레이어가 안내 HUD보다 위인지도 검사했습니다.
- 브라우저에 표시한 픽셀 좌표와 세 Python 엔진/WebGazer의 학습 좌표가 같은 서버 응답에서 나오는지 확인했습니다. 화면 크기로 다시 곱하는 이전 경로가 남아 있지 않으며, DOM 배치 뒤 실제 사각형을 검사하는 코드도 확인했습니다.
- 자동 로그 테스트는 네이티브 호출 직전에 단계가 디스크에 기록되는지, 예외 단계/엔진이 보존되는지, 비공개 영상·토큰·프레임 해시가 로그로 유출되지 않는지 확인했습니다. 성공 시 진단 타이머 취소, 파일 쓰기 실패, 네이티브 핸들러를 사용할 수 없는 경우, 재시작 직전 단계 보존도 확인했습니다. faulthandler는 모형으로 대체하며 실제 충돌을 발생시키지 않았습니다.
- JavaScript 연결 검사 20개: HTTP 원문, 연결/시간 초과/JSON/404 구분, 본문 수신 시간 제한, 외부 요청 거부, 원본 0바이트 파일, 누락/잘린 파일, 로딩 오류 표시를 확인했습니다. 동일 응답 복구는 GET만 사용하고 최대 시도 횟수가 제한되는지, 복구 불가 시 상태 확인을 GET 한 번만 수행하고 POST를 재시도하지 않는지, 서버 연결 여부를 모델 실패 원인으로 단정하지 않는지 검사했습니다. 내부 읽기 전용 진단 함수는 유지하지만 GUI의 수동 진단 버튼은 제거했습니다.
- 원본 WebGazer Ridge 수학 검사 6개: 초기 270표본 유지, 실제 클릭 학습 후 예측 변화, 마우스 이력과 50ms 간격, 오래된 눈 특징 거부, 모델 전환 후 학습 상태 유지, 버퍼 상한/명시적 재보정 초기화. 이미지 특징만 모형 숫자로 대체했으며 실제 원본 회귀 계산기를 사용합니다.
- 복구 검사에서는 실제 HTTP 요청 파서·응답 생성기를 인메모리 스트림으로 호출했습니다. 소켓 생성을 금지하고 가짜 모델을 사용했습니다. JSON 본문 또는 헤더 전송 중 연결 끊김을 발생시킨 뒤 저장된 응답이 정확히 복구되는지, 모델 호출/승인 표본/fit 횟수가 늘지 않는지 검사했습니다. 인증·세션·프레임 불일치, 캐시 상한·종료 시 삭제, pending/오류 구분, 동일 연결에서 연속 HTTP/1.1 요청의 바이트 길이, 본문을 읽지 않은 거부 요청의 연결 종료도 검사했습니다.
- 브라우저 모형 검사는 같은 번호 응답 복구, 본문 수신 실패의 HTTP 상태 보존, pending 뒤 완료, 없는 응답·다른 세션·다른 번호 거부, 실제 HTTP 오류 유지, 재시도 상한, 승인되지 않은 표본의 거부 상태 보존을 포함합니다.
- Python/JavaScript 문법, HTML 요소 참조, 제거한 평가 기능으로의 남은 호출이 없는지 검사했습니다.

## 모델 파일과 배포

- 고정 WebGazer npm 3.5.3 원본 아카이브와 동봉 브라우저 리소스 20개를 바이트 비교했습니다. JS, SIMD/비 SIMD WASM, graph, packed data, 소스맵, 원본 0바이트 data를 포함합니다.
- EyeTrax FaceLandmarker 모델은 고정 공식 모델 URL, GazeFollower `base.mnn`은 설치 배포물 RECORD의 해시와 비교한 원본을 유지했습니다. 원본 아카이브·라이선스·해시는 `third_party/`와 `THIRD_PARTY_NOTICES.md`에 있습니다.
- 모델 경로 수정은 EyeTrax 0.4.0 원본 생성자/옵션/상속 메서드와 비교합니다. 실제 설치된 MediaPipe 0.10.21의 `BaseOptions.to_pb2()`에서도 모델 바이트가 file_content로 전달되고 file_name은 설정되지 않음을 모델 생성 없이 확인한 경로입니다.
- `build_zip.py`는 바이너리를 포함한 전체 폴더를 수집합니다. ZIP을 임시 폴더에 새로 풀어 전체 파일 목록, 크기, SHA-256, CRC, 필수 브라우저 파일을 재확인합니다. 원본부터 빈 파일과 누락/손상된 파일을 구분합니다.
- Python 인터프리터와 OS별 pip 패키지 설치본은 ZIP에 포함하지 않습니다. 사용자가 Python 3.11 환경에 `requirements.txt`를 설치해야 합니다. 이미 설치했다면 이번 변경을 위해 다시 설치할 필요 없습니다.

## 재현 명령

아래는 개발 검증용이며 앱 실행에 필요한 절차나 GUI의 평가 기능이 아닙니다.

```bash
python verify_bundle.py
python verify_bundle.py --packages
python -m unittest discover -s tests -v
node --test tests/test_network_diagnostics.mjs
node tests/test_webgazer_math.mjs
```

Node는 앱 실행에 필요하지 않습니다. 일반 사용자는 `run.py`만 실행하고 브라우저에 직접 접속하면 됩니다.

**앱·서버·브라우저·웹캠·실제 모델 추론은 제작 과정에서 실행하지 않았습니다.** 실제 Windows 카메라 인식, 전체 보정 성공, 추적 정확도·속도, 네이티브 충돌의 재현/해결은 검증하지 않았습니다. 현재 제공된 로그로 `/api/frame` 실패 원인을 확정할 수 없습니다. 모형 테스트의 통과는 실제 장치에서 네 엔진의 정상 작동을 보장하지 않습니다.
